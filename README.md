# BankRest Service

This application is a comprehensive bank card management system

### Local Launch
In the root directory, open the console and run command:
`docker compose up -d`

The application runs on port 8080.

### Swagger
http://localhost:8080/bank/swagger-ui.html

